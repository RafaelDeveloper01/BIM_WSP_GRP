## INSTALACION
```
npm install

npm run build

npm run dev
```

docker run --privileged -e PORT=3001 -p 3001:3001 apiwhatsappts:latest

subir 
git push origin main   

descargar
git pull origin main

exportar a git : luego de validar q exista en variables globales

export PATH=$PATH:/c/Program\ Files/nodejs

-- elimianr el node_modules y volver a instalar node cualqueir cosa